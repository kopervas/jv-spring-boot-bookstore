databaseChangeLog:
  - changeSet:
      id: set-default-role-for-users
      author: serg.kopervas
      changes:
        - sql:
            sql: |
              INSERT INTO users_roles (user_id, role_id)
              SELECT u.id, r.id
              FROM users u
              CROSS JOIN roles r
              WHERE r.name = 'ROLE_USER'
              AND NOT EXISTS (
                  SELECT 1 FROM users_roles ur
                  WHERE ur.user_id = u.id AND ur.role_id = r.id
              );

        - sql:
            sql: |
              INSERT INTO users_roles (user_id, role_id)
              SELECT u.id, r.id
              FROM users u
              JOIN roles r ON r.name = 'ROLE_ADMIN'
              WHERE u.email = 'admin@example.com'
              AND NOT EXISTS (
                  SELECT 1 FROM users_roles ur
                  WHERE ur.user_id = u.id AND ur.role_id = r.id
              );